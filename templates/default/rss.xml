<rss version="2.0">
    <channel>
        <title>Recently Published Archives</title>
        <link>http://authors.example.edu</link>
        <description>Recently Published Arctiles added to the Author's repository</description>
        {{range . }}
        <item>
            <title>{{.Title}}</title>
            <link>{{.OfficialURL}}</link>
            <description>
                {{ if .Creators }}Authors: {{ range $i, $name := .Creators -}} {{if (gt $i 1) }}, {{end}} {{$name.Given}} {{$name.Family}} {{ end -}}{{ end }}
                {{with .Publication}}Publication: {{ . }}{{ end -}} {{with .Volume}}Vol.: {{ . }}{{ end -}} {{with .Number}}No.: {{ . }} {{ end -}} {{with .PageRange}}Pages {{ . }}{{ end -}}
                {{with .Date}}Published: {{.}}{{ end }}
                {{with .ISSN}}ISSN: {{ . }}{{ end }}
                {{with .IDNumber }}ID: {{ . }}{{ end }}
                {{with .OfficialURL }}Official URL: <a href="{{ . }}">{{ . }}</a>{{ end }}
            </description>
            <pubDate>{{rfc882 .Date}}</pubDate>
            <guid>{{.IDNumber}}</guid>
        </item>
        {{ end }}
    </channel>
</rss>
