<rss version="2.0">
    <channel>
        <title>{{.DocTitle}}</title>
        <link>{{ .SiteURL }}/{{ .Basepath }}/rss.xml</link>
        <description>{{ .DocDescription }}</description>
        <lastBuildDate>{{rfc1123z "now"}}</lastBuildDate>
        <docs>http://cyber.law.harvard.edu/rss/rss.html</docs>
        <generator>epgo {{ .Version }}</generator>
        {{- $ApiURL := .ApiURL -}}
        {{range .Records }}
        <item>
            <title>{{ .Title }}</title>
            <link>{{ .OfficialURL }}</link>
            <description><![CDATA[
                {{ if .Creators }}<p>Authors: {{ range $i, $name := .Creators -}} {{if (gt $i 1) }}, {{end}} {{$name.Given}} {{$name.Family}} {{ end -}}</p>{{ end }}

                {{with .Abstract }}<p>Abstract: {{ . }}</p>{{ end }}

                {{with .Publication}}Publication: {{ . }}{{ end -}} {{with .Volume}} Vol.: {{ . }}{{ end -}} {{with .Number}} No.: {{ . }} {{ end -}} {{with .PageRange}} Pages {{ . }}<br />{{ end -}}
                {{with .ISSN}}ISSN: {{ . }}<br />{{ end }}
                {{with .IDNumber }}ID: {{ . }}<br />{{ end }}
                ]]>
            </description>
            <pubDate>{{rfc1123z .Date}}</pubDate>
            <guid>{{ $ApiURL }}{{ .URI }}</guid>
        </item>
        {{ end }}
    </channel>
</rss>
